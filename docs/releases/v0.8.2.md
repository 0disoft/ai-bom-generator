## Highlights

Update uv to 0.12.15 for CI, publishing and the composite Action, and Bun to
1.4.2 for release-tooling CI. Verify actual compatible SPDX outputs with the
independent SPDX Python model, including typed relationships and preserved
evidence. Check security support documentation against the current minor line.

## Compatibility

No CLI, config or Action input changes. SPDX consumer verification is CI-only;
no new runtime dependency or additional SPDX profile support is introduced.
Security support remains the latest released minor line, currently 0.8.x.

## Rollback

Pin the immutable v0.8.1 Action or install ai-bom-generator==0.8.1 if the new
toolchain causes a regression. Promote v0 only after published-package and
exact-version external Action verification. Never rewrite immutable tags or
delete published distributions.
