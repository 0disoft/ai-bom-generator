name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validation:
    name: Python 3.12 validation
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7.0.0

      - name: Set up Python
        uses: actions/setup-python@v6.3.0
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Install locked environment
        run: uv sync --locked

      - name: Compile sources
        run: uv run --python 3.12 python -m compileall -q src tests

      - name: Run unit and contract tests
        run: uv run --python 3.12 python -m unittest discover -s tests -v

      - name: Smoke CLI fixture
        run: |
          set -euo pipefail
          out_dir="$(mktemp -d)"
          uv run --python 3.12 python -m ai_bom_generator generate \
            tests/fixtures/complete-project \
            --config tests/fixtures/complete-project/aibom.toml \
            --format cyclonedx-json-1.7 \
            --output "$out_dir/bom.cdx.json" \
            --warning-report "$out_dir/warnings.json" \
            --summary "$out_dir/summary.json"
          test -s "$out_dir/bom.cdx.json"
          test -s "$out_dir/warnings.json"
          test -s "$out_dir/summary.json"

      - name: Check repository diff hygiene
        run: git diff --check
